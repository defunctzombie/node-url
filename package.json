{
  "name": "url",
  "description": "The core `url` packaged standalone for use with Browserify.",
  "version": "0.8.0",
  "dependencies": {
    "punycode": "^1.2.4"
  },
  "devDependencies": {
    "assert": "^1.1.1"
  }
}
